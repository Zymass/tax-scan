# Где смотреть логи

## Backend логи (сервер)

Логи backend сервера выводятся в **терминал**, где запущен сервер.

### Как запустить сервер и увидеть логи:

1. Откройте терминал
2. Перейдите в директорию backend:
   ```bash
   cd backend
   ```
3. Запустите сервер:
   ```bash
   npm run dev
   ```
4. Логи будут появляться в этом терминале

### Что искать в логах:

При попытке скачать PDF, ищите сообщения с префиксами:
- `[PDF]` - общий процесс генерации PDF
- `[PDF Generator]` - детали работы Puppeteer

Пример логов:
```
[PDF] Starting PDF generation for calculation abc123
[PDF] Calculation loaded: { id: 'abc123', status: 'completed' }
[PDF] Recalculating results...
[PDF Generator] Launching Puppeteer...
[PDF Generator] PDF generated successfully, size: 12345 bytes
```

Если есть ошибка:
```
❌ [2024-01-01T12:00:00.000Z] Error 500: PDF generation failed: ...
📍 Path: GET /api/calculations/abc123/pdf
📚 Stack: ...
```

## Frontend логи (браузер)

Логи фронтенда выводятся в **консоль браузера**.

### Как открыть консоль:

1. Откройте приложение в браузере
2. Нажмите `F12` (или `Cmd+Option+I` на Mac)
3. Перейдите на вкладку **Console**
4. Там будут все ошибки и запросы к API

### Что искать:

- Ошибки с красным цветом
- Сообщения типа `Error exporting PDF: ...`
- Запросы к `/api/calculations/{id}/pdf`

## Полезные команды для отладки

### Просмотр последних логов сервера:
Если сервер запущен в фоне, можно использовать:
```bash
# В macOS/Linux можно использовать tail для просмотра логов
# Но в нашем случае логи идут в консоль, где запущен процесс
```

### Проверка, что сервер работает:
```bash
curl http://localhost:3000/health
```

### Проверка конкретного запроса:
Откройте Network вкладку в DevTools браузера (F12), найдите запрос к `/pdf` и посмотрите:
- Status Code (должен быть 200 для успеха, 500 для ошибки)
- Response - там будет детальная ошибка
